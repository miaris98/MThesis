carla==0.9.15
numpy
pillow
opencv-python
torch
torchvision
timm  # required by both venv_train (world_on_rails CARLA encoder) and venv_carla (bench2drive_agent.py loads the same encoder)
gymnasium
tensorboard
mlflow
